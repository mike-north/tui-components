## API Report File for "@tuicomponents/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Color as ChromatermColor } from 'chromaterm';
import { Theme as ChromatermTheme } from 'chromaterm';
import { ThemeOptions } from 'chromaterm';
import { ZodType } from 'zod';
import { ZodTypeDef } from 'zod';

// @public
export function anchorLine(content: string, anchor?: string): string;

// @public
export function applyMarkdownStyle(text: string, style: MarkdownStyle): string;

// @public
export abstract class BaseTuiComponent<TInput, TSchema extends ZodType<TInput, ZodTypeDef, unknown>> implements TuiComponent<TInput, TSchema> {
    // (undocumented)
    getJsonSchema(): object;
    // (undocumented)
    abstract readonly metadata: ComponentMetadata<TInput>;
    // (undocumented)
    abstract render(input: TInput, context: RenderContext): RenderResult;
    // (undocumented)
    abstract readonly schema: TSchema;
}

export { ChromatermColor }

export { ChromatermTheme }

// @public
export interface ComponentExample<TInput> {
    description?: string;
    input: TInput;
    name: string;
}

// @public
export type ComponentFactory<TInput, TSchema extends ZodType<TInput, ZodTypeDef, unknown>> = () => TuiComponent<TInput, TSchema>;

// @public
export interface ComponentInfo {
    // (undocumented)
    description: string;
    // (undocumented)
    name: string;
    // (undocumented)
    version: string;
}

// @public
export interface ComponentMetadata<TInput> {
    description: string;
    examples: ComponentExample<TInput>[];
    name: string;
    supportedModes?: RenderMode[];
    version: string;
}

// @public
export class ComponentRegistry {
    clear(): void;
    // Warning: (ae-forgotten-export) The symbol "AnyComponent" needs to be exported by the entry point index.d.ts
    get(name: string): AnyComponent | undefined;
    has(name: string): boolean;
    list(): ComponentInfo[];
    names(): string[];
    register<TInput, TSchema extends ZodType<TInput, ZodTypeDef, unknown>>(factory: () => TuiComponent<TInput, TSchema>): TuiComponent<TInput, TSchema>;
}

// @public
export function createRenderContext(options?: CreateRenderContextOptions): RenderContext;

// @public
export interface CreateRenderContextOptions {
    autoDetectMode?: boolean;
    noColor?: boolean;
    renderMode?: RenderMode;
    theme?: TuiTheme;
    width?: number;
}

// @public
export function createStyleFunctions(renderMode: RenderMode, theme?: TuiTheme): StyleFunctions;

// @public
export function createThemeSync(): TuiTheme;

// @public
export const DEFAULT_ANCHOR = "\u2502";

// @public (undocumented)
export const DEFAULT_TERMINAL_HEIGHT = 24;

// @public
export const DEFAULT_TERMINAL_WIDTH = 80;

// @public
export const defaultTheme: TuiTheme;

// @public
export function detectColorLevel(): 0 | 1 | 2 | 3;

// @public
export function detectEnvironment(): EnvironmentDetection;

// @public
export function detectTheme(options?: ThemeOptions): Promise<TuiTheme>;

// @public
export interface EnvironmentDetection {
    confidence: "high" | "medium" | "low";
    detectedAssistant?: string;
    isAIAssistant: boolean;
    processTree: ProcessAncestor[];
}

// @public
export function getMarkdownRenderedWidth(str: string): number;

// @public
export function getProcessTree(): ProcessAncestor[];

// @public
export function getStringWidth(str: string): number;

// @public
export function getTerminalSize(): {
    columns: number;
    rows: number;
};

// @public
export function getTerminalWidth(): number;

// @public
export function inlineCode(text: string): string;

// @public
export function isRunningInAIAssistant(): boolean;

// @public
export function isTTY(): boolean;

// @public
export function joinAnchoredLines(lines: string[], anchor?: string): string;

// @public
export type MarkdownStyle = "primary" | "secondary";

// @public
export function measureLines(str: string): {
    lines: string[];
    maxWidth: number;
    lineCount: number;
};

// @public
export interface PadOptions {
    align?: "left" | "right" | "center";
    padChar?: string;
}

// @public
export function padToWidth(str: string, targetWidth: number, options?: PadOptions): string;

// @public
export interface ProcessAncestor {
    // (undocumented)
    command: string;
    // (undocumented)
    pid: number;
}

// @public
export const registry: ComponentRegistry;

// @public
export interface RenderContext {
    colorLevel: 0 | 1 | 2 | 3;
    isTTY: boolean;
    renderMode: RenderMode;
    style: StyleFunctions;
    theme?: TuiTheme;
    width: number;
}

// @public
export type RenderMode = "ansi" | "markdown";

// @public
export interface RenderResult {
    actualWidth: number;
    lineCount: number;
    output: string;
}

// @public
export interface SemanticColors {
    added: ChromatermColor;
    addedBackground: ChromatermColor;
    border: ChromatermColor;
    error: ChromatermColor;
    header: ChromatermColor;
    info: ChromatermColor;
    modified: ChromatermColor;
    primary: ChromatermColor;
    removed: ChromatermColor;
    removedBackground: ChromatermColor;
    secondary: ChromatermColor;
    success: ChromatermColor;
    warning: ChromatermColor;
}

// @public
export function stripMarkdownFormatting(str: string): string;

// @public
export interface StyleFunctions {
    border: (text: string) => string;
    error: (text: string) => string;
    header: (text: string) => string;
    info: (text: string) => string;
    primary: (text: string) => string;
    secondary: (text: string) => string;
    success: (text: string) => string;
    warning: (text: string) => string;
}

export { ThemeOptions }

// @public
export interface TruncateOptions {
    ellipsis?: string;
    position?: "end" | "middle";
}

// @public
export function truncateToWidth(str: string, targetWidth: number, options?: TruncateOptions): string;

// @public
export interface TuiComponent<TInput, TSchema extends ZodType<TInput, ZodTypeDef, unknown> = ZodType<TInput, ZodTypeDef, unknown>> {
    getJsonSchema(): object;
    readonly metadata: ComponentMetadata<TInput>;
    render(input: TInput, context: RenderContext): RenderResult;
    readonly schema: TSchema;
}

// @public
export interface TuiTheme {
    chromaterm: ChromatermTheme;
    semantic: SemanticColors;
}

// @public
export interface WrapOptions {
    hard?: boolean;
    trim?: boolean;
    wordWrap?: boolean;
}

// @public
export function wrapText(text: string, width: number, options?: WrapOptions): string;

// @public
export function wrapTextWithInfo(text: string, width: number, options?: WrapOptions): {
    text: string;
    lines: string[];
    lineCount: number;
    maxWidth: number;
};

// (No @packageDocumentation comment for this package)

```
